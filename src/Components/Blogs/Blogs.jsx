const Blogs = () => {
    return (
        <div>
            <h3 className="text-center text-2xl font-bold">A blog (a truncation of "weblog") is an informational website consisting of discrete, often informal diary-style text entries (posts). Posts are typically displayed in reverse chronological order so that the most recent post appears first, at the top of the web page.</h3>
        </div>
    );
};

export default Blogs;